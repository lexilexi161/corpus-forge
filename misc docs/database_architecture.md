# Database Architecture

The current database architecture documentation lives at:

```text
documentation/database_architecture.md
```

This older `misc docs` file is kept only as a pointer so teammates do not accidentally update two different database documents before submission.
